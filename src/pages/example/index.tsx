import {ContainerStepsEditUser} from '@/components';

const Index = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
      }}
    >
      <ContainerStepsEditUser />
    </div>
  );
};

export default Index;

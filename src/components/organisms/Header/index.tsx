import {HeaderLogoContainer, HeaderNavigation} from '@/components';

import {Container} from './style';

export const Header = () => {
  return (
    <Container>
      <HeaderLogoContainer />
      <HeaderNavigation />
    </Container>
  );
};
